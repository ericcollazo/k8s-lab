apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  ports:
  - nodePort:
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
  type: NodePort
status:
  loadBalancer: {}